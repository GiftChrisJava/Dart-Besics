void main() {
  // interpolation
  const name = "Ray";
  const introduction = "Hello my name is $name";
  print(introduction);

  // controlling the number of decimal numbers
  const oneThird = 1 / 3;
  final sentance = 'one third is ${oneThird.toStringAsFixed(3)}';
  print(sentance);
}
